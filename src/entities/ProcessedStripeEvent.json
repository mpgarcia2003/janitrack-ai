{
  "name": "ProcessedStripeEvent",
  "type": "object",
  "properties": {
    "stripe_event_id": {
      "type": "string",
      "description": "Unique Stripe event ID. Idempotency key."
    },
    "event_type": {
      "type": "string"
    },
    "processed_at": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": ["stripe_event_id", "event_type", "processed_at"]
}
